resources:
  jobs:
    secret_scopes_job:
      name: secret_scopes_job
      tasks:
        - task_key: read_secret
          notebook_task:
            notebook_path: ../../src/read_secrets.ipynb
            source: WORKSPACE
      parameters:
        - name: secret-scope
          default: ${resources.secret_scopes.my_secret_scope.name}